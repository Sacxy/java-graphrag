package com.tekion.javaastkg.agents.models;

/**
 * Types of code entities that can be extracted and analyzed
 */
public enum EntityType {
    CLASS,
    INTERFACE,
    METHOD,
    FIELD,
    PACKAGE,
    ANNOTATION,
    ENUM,
    RECORD,
    VARIABLE,
    PARAMETER
}